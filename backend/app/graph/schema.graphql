schema {
  query: Query
  mutation: Mutation
  # subscription: Subscription
}

type Mutation {
  createTask(input: NewTask!): Task!
  updateTask(input: UpdateTask!): Task!
}

type Query {
  tasks: [Task]!
  task(id: ID!): Task!
}


interface Node {
  id: ID!
}

scalar Time
